import {View} from "react-native";
import * as React from 'react';

export default function Auteur({route}){
    const { itemId, otherParam } = route.params;
    return(
        <View>
        </View>
    )
}